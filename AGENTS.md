# AGENTS.md

このRepositoryでCodex/AI Agentが作業する際の共通ルール。個別仕様はGitHub Issueを正とし、このファイルには毎回共通するルールだけを置く。

## 1. 優先順位

1. トークン消費を最小化する
2. 品質・精度・安全性を落とさない
3. ユーザーの指示回数を減らす

判断に迷う場合は、最小変更・既存仕様維持・既存実装の再利用を優先する。

## 2. 作業開始時

- 対象Issueを最初に読む。
- 親Goalが指定されている場合だけ親Issueも読む。
- Repository全体を無目的に走査しない。まず対象Issueに関係するファイル、既存テスト、関連docsだけ確認する。
- 既存コード・型・utility・service・test helperを検索し、再利用できるものを優先する。
- 仕様がIssue内で十分なら確認質問をせず実装する。
- 仕様上の重大な矛盾、安全性の問題、不可逆な変更だけ質問する。

## 3. 実装ルール

- Issue単位でbranchを分ける。
- Goal外・Issue外の機能を勝手に追加しない。
- 変更範囲は必要最小限にする。
- 同じ処理を新しく作る前に既存実装を探す。
- 不要な抽象化、将来用の過剰設計、不要な依存追加を避ける。
- 日付計算、スコア計算、重複排除、権限制御など決定論的処理は通常コードで実装する。AIに任せない。
- AIは文章理解・分類・要約など、非決定論的処理に限定する。
- 既存挙動を変更する場合は、その変更を固定するテストを追加する。

## 4. セキュリティ必須

- `contacts`等の個人情報をAI分析処理へ渡さない。
- 担当者名、電話番号、メールアドレス等のPIIを外部AIへ送信しない。
- Service Role Key、API Key、Secretsをclient bundle・ログ・Repositoryへ出さない。
- Supabase RLSとサーバー側認可を維持する。
- IDOR、組織境界、owner/admin権限を壊さない。
- 入力値はサーバー側でZod等を使って検証する。
- ログへPII・Secretsを残さない。
- 新しい外部サービス・API・依存を追加する場合は、無料/低コスト、利用規約、ライセンス、セキュリティを確認する。

詳細は`docs/security.md`を参照する。

## 5. DB / Migration

- Schema変更はMigrationで管理する。
- MigrationはSupabase/PostgreSQLへ実適用できることをテストする。
- RLS/RBAC変更時は実DBテストを追加する。
- DB制約とAPI/Zod検証の意味を揃える。
- Migrationを過去修正せず、新しいMigrationで前進する（未マージbranch内の作業を除く）。

## 6. テスト

実装中は対象範囲の最小テストを優先して回す。PR前に以下を実行する。

- TypeScript typecheck
- ESLint
- Unit / relevant security tests
- Production build
- Secret / client bundle scan
- `npm audit`
- DB変更時はSupabase Migration適用 + 実DB RLS/RBAC test

既存CIが同じ検証を行う場合はCIを正とし、同じ検証結果を長文で再掲しない。

## 7. GitHub運用

- Issue → branch → commit → PR → CI → review → merge の順で進める。
- PR本文に対象Issueを紐付ける。
- README/docsは、実装によって内容が変わる場合だけ更新する。
- PR作成後はユーザー承認なしにmainへマージしない。
- PRレビュー対応中は次Issueへ進まない。

## 8. トークン節約ルール

- Issue本文を回答内で繰り返さない。
- 読んだファイル内容を丸ごと要約しない。
- 大きなdiffやファイル全文をチャットへ貼らない。
- 調査結果は実装判断に必要なものだけ報告する。
- 既にCIで確認できる成功項目を逐一説明しない。
- 途中報告は、問題・判断変更・ブロッカーがある場合だけ行う。
- コードコメントは「なぜ」が必要な非自明部分だけにする。
- docsの重複を増やさず、既存docsを更新する。

## 9. 完了報告

PR作成後の報告は原則として次だけでよい。

1. PR番号 / URL
2. 主な変更（最大5項目）
3. テスト結果（成功/失敗と失敗理由）
4. 残るリスクまたは未確認事項
5. mainへ未マージであること

問題がなければ長い実装解説は不要。

## 10. 通常のCodex指示

このファイルがmainに存在する場合、通常は以下の短い指示で開始する。

`Issue #NをAGENTS.mdに従って実装し、テスト後にPR作成まで進めてください。mainへはマージしないでください。`

Issueに完了条件が十分書かれている場合、それ以上の詳細指示を要求しないこと。
